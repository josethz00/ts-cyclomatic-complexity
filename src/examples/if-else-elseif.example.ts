/**
 *
 * @param a
 * @param b
 * @returns
 */
export function exampleFunction(a: number, b: number): number {
  if (a > b) {
    return a;
  } else if (a < b) {
    return b;
  } else {
    return 0;
  }
}
