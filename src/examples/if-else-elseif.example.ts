/**
 *
 * @param a
 * @param b
 * @returns
 */
export function exampleFunction(a: number, b: number): number {
  if (a > b) {
    return a; // 1
  } else if (a < b) {
    return b; // 2
  } else {
    return 0; // 3
  }
}
